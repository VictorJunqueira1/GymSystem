# Entidades do domínio (ex.: Exercício, Treino).
# Invariantes e regras internas das entidades (ex.: campos obrigatórios, consistência).
# Identidade (ID) como atributo, relacionamento por IDs (simples e rastreável).